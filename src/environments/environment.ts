// Configurazione runtime per le build di produzione; evita di inserire segreti nel bundle.
// Suggerimento: estrai gli URL per ambiente tramite file environment di Angular.
export const environment = {
  production: true,
  rooturl : 'http://localhost:8080'
};
