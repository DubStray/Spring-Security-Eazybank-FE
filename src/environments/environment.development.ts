// Configurazione per sviluppo locale; Angular la usa durante `ng serve`.
// Ottimo posto per puntare a mock o servizi di staging quando serve.
export const environment = {
  production: false,
  rooturl : 'http://localhost:8080'
};
